import React, { Fragment } from "react";
import Navbar from "../Navbar";

const Hero = () => {
  return (
    <Fragment>
      <Navbar />
      <header>Header</header>
    </Fragment>
  );
};

export default Hero;
